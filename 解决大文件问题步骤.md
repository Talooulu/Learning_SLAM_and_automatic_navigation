# 解决 GitHub 推送大文件错误

## 错误信息
```
remote: error: File car.dae is 123.00 MB; 
this exceeds GitHub's file size limit of 100.00 MB
```

## 问题原因
GitHub 不允许上传超过 100MB 的单个文件。`car.dae` 文件有 123MB，超过了限制。

## 解决方案

### 方案1：忽略大文件（推荐，最简单）

#### 步骤1：已经从 Git 中移除（保留本地文件）

```bash
cd /home/talooulu/ROS2/Pure-tracking-slam-automatic-navigation-system

# 从 Git 中移除大文件（但保留本地文件）
git rm --cached "src/su7_gazebo/gazebo_four_wheeled_vehicle/four_wheeled_vehicle_plugin/models/four_wheeled_vehicle/meshes/car.dae"
```

#### 步骤2：确认 .gitignore 已更新

我已经更新了 `.gitignore` 文件，添加了：
```
*.dae
**/meshes/*.dae
src/su7_gazebo/**/*.dae
```

#### 步骤3：提交删除操作

```bash
# 提交移除大文件的操作
git add .gitignore
git commit -m "移除大文件 car.dae，添加到 .gitignore

- 文件超过 GitHub 100MB 限制
- 使用 .gitignore 忽略所有 .dae 文件"
```

#### 步骤4：推送到 GitHub

```bash
git push -u origin masters
```

---

### 方案2：使用 Git LFS（如果需要保留文件在 GitHub）

如果你**必须**把这个大文件上传到 GitHub（比如需要别人下载），可以使用 Git LFS。

#### 步骤1：安装 Git LFS

```bash
sudo apt update
sudo apt install git-lfs
```

#### 步骤2：初始化 Git LFS

```bash
cd /home/talooulu/ROS2/Pure-tracking-slam-automatic-navigation-system
git lfs install
```

#### 步骤3：跟踪 .dae 文件

```bash
# 告诉 Git LFS 管理所有 .dae 文件
git lfs track "*.dae"
git lfs track "**/*.dae"

# 添加 .gitattributes 文件
git add .gitattributes
```

#### 步骤4：从 Git 历史中移除大文件，用 LFS 重新添加

```bash
# 移除已提交的大文件
git rm --cached "src/su7_gazebo/**/*.dae"

# 重新添加（这次会使用 LFS）
git add "src/su7_gazebo/**/*.dae"

# 提交
git commit -m "使用 Git LFS 管理大文件 .dae"
```

#### 步骤5：推送到 GitHub

```bash
git push -u origin masters
```

**注意**：Git LFS 在 GitHub 上有配额限制（免费账号 1GB 存储，1GB/月流量）

---

### 方案3：删除大文件（如果不需要）

如果你不需要这个 `car.dae` 文件，可以直接删除：

```bash
# 从 Git 中移除并删除本地文件
git rm "src/su7_gazebo/**/*.dae"

# 提交
git commit -m "删除大文件 car.dae"

# 推送
git push -u origin masters
```

---

## 推荐操作（方案1）

我已经帮你更新了 `.gitignore`，现在执行：

```bash
cd /home/talooulu/ROS2/Pure-tracking-slam-automatic-navigation-system

# 1. 从 Git 中移除大文件（保留本地文件）
git rm --cached "src/su7_gazebo/gazebo_four_wheeled_vehicle/four_wheeled_vehicle_plugin/models/four_wheeled_vehicle/meshes/car.dae"

# 2. 添加更新后的 .gitignore
git add .gitignore

# 3. 提交
git commit -m "移除大文件，添加到 .gitignore"

# 4. 推送
git push -u origin masters
```

---

## 验证

推送成功后，可以在 GitHub 上验证：
- 打开仓库页面
- 检查 `src/su7_gazebo/.../meshes/` 目录
- 应该看不到 `car.dae` 文件（因为被 .gitignore 忽略了）

---

## 总结

- **最简单**：使用方案1（忽略大文件）
- **如果需要文件在 GitHub**：使用方案2（Git LFS）
- **不需要文件**：使用方案3（删除）

推荐**方案1**，因为：
1. 3D 模型文件通常很大，不需要版本控制
2. 其他人可以从其他地方获取模型文件
3. 避免占用 GitHub 存储空间

