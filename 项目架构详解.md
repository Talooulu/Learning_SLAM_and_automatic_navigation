# Pure-tracking-slam-automatic-navigation-system 项目架构详解

> 本文档面向编程小白，详细解释整个项目的架构、节点功能、数据流和协作逻辑

---

## 📋 目录

1. [项目概述](#项目概述)
2. [整体架构](#整体架构)
3. [包结构说明](#包结构说明)
4. [节点详解](#节点详解)
5. [数据流图](#数据流图)
6. [运行流程](#运行流程)
7. [关键算法](#关键算法)

---

## 项目概述

这是一个**基于ROS2的差速机器人SLAM建图与自动导航系统**，主要功能包括：

- **仿真环境**：使用Gazebo进行机器人仿真
- **SLAM建图**：基于点云数据构建2D地图
- **路径规划**：使用A*算法规划路径
- **路径跟踪**：使用纯追踪（Pure Pursuit）算法控制机器人跟随路径

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      ROS2 通信框架                            │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌────▼────┐        ┌─────▼─────┐      ┌─────▼─────┐
   │ Gazebo  │        │  TF变换   │      │  导航节点  │
   │ 仿真包  │        │  节点组   │      │   组      │
   └────┬────┘        └─────┬─────┘      └─────┬─────┘
        │                   │                   │
        │ 传感器数据         │ 坐标变换          │ 控制指令
        │                   │                   │
┌───────▼───────────────────▼───────────────────▼───────┐
│                                                       │
│  1. 点云处理 → 2. 地图构建 → 3. 路径规划 → 4. 路径跟踪 │
│                                                       │
└───────────────────────────────────────────────────────┘
```

---

## 包结构说明

### 1. `gazebo_modele` 包 - 仿真环境

**作用**：提供机器人仿真环境和基础TF变换

**包含文件**：
- `gazebo.launch.py` - 启动Gazebo仿真
- `model.urdf` - 机器人模型描述文件
- `3d.world` / `2d.world` - Gazebo世界文件

**启动的节点**：
1. **Gazebo服务器** - 运行仿真环境
2. **spawn_entity** - 在Gazebo中生成机器人模型
3. **robot_state_publisher** - 发布机器人关节状态
4. **joint_state_publisher_gui** - 可视化关节状态
5. **static_transform_publisher** (3个) - 发布静态TF变换

### 2. `nav_slam` 包 - 导航与SLAM

**作用**：实现SLAM建图、路径规划和路径跟踪

**包含的节点**：
1. `points_pub_map` - 点云坐标变换
2. `map_pub` - 地图构建与发布
3. `odom_map_tf` - 里程计到地图的TF变换
4. `astar` - A*路径规划
5. `start_nav` - 纯追踪路径跟踪控制

---

## 节点详解

### 🔵 仿真层节点（gazebo_modele包）

#### 1. Gazebo服务器
- **功能**：运行3D仿真环境，模拟物理世界
- **输出**：
  - `/points_raw` - 激光雷达点云数据
  - `/odom` - 机器人里程计数据（位置、速度）
  - `/imu` - IMU传感器数据

#### 2. robot_state_publisher
- **功能**：根据URDF模型发布机器人各关节的TF变换
- **输出**：TF树（base_link → 各个关节）

#### 3. static_transform_publisher (3个)
- **功能**：发布静态坐标变换
- **变换关系**：
  - `map → odom` - 地图到里程计的初始变换
  - `odom → base_footprint` - 里程计到机器人脚部
  - `base_footprint → base_link` - 脚部到机器人本体（高度0.1m）

---

### 🟢 导航层节点（nav_slam包）

#### 1. `points_pub_map` - 点云坐标变换节点

**文件**：`nav_slam/points_pub_map.py`

**功能**：
- 将激光雷达点云从`base_link`坐标系转换到`map`坐标系
- 这样所有点云都在统一的地图坐标系中，便于建图

**订阅**：
- `/points_raw` - 原始点云（base_link坐标系）
- `/odom` - 机器人当前位置和姿态

**发布**：
- `/mapokk` - 转换后的点云（map坐标系）

**工作原理**：
```python
1. 接收点云数据（相对于机器人）
2. 获取机器人当前位姿（从/odom）
3. 构建旋转矩阵（从四元数计算）
4. 将点云坐标乘以变换矩阵
5. 发布转换后的点云
```

**为什么需要**：
- 激光雷达数据是相对于机器人的，但建图需要统一坐标系
- 通过坐标变换，所有点云都转换到地图坐标系

---

#### 2. `map_pub` - 地图构建节点

**文件**：`nav_slam/map_pub.py`

**功能**：
- 将点云数据转换为2D占用栅格地图（OccupancyGrid）
- 实现障碍物膨胀（多层安全区域）

**订阅**：
- `/mapokk` - 转换后的点云（map坐标系）
- `/odom` - 机器人位置（用于确定地图原点）

**发布**：
- `/combined_grid` - 2D占用栅格地图

**工作原理**：
```python
1. 接收点云数据
2. 过滤高度范围（0.1m - 1.0m，只保留障碍物高度）
3. 将3D点投影到2D网格：
   - 地图大小：60m × 60m
   - 分辨率：0.1m（每个格子10cm）
   - 共 600 × 600 = 360,000 个格子
4. 标记障碍物：
   - 值 = 100：障碍物（黑色）
   - 值 = 5：第一层膨胀（安全距离）
   - 值 = -8：第二层膨胀
   - 值 = -120：第三层膨胀
   - 值 = 1：自由空间（白色）
   - 值 = -1：未知区域（灰色）
5. 发布地图
```

**关键参数**：
- `grid_width = 60.0` - 地图宽度（米）
- `grid_height = 60.0` - 地图高度（米）
- `resolution = 0.1` - 分辨率（米/像素）
- `obstacle_radius = 0.2` - 障碍物膨胀半径

**为什么需要多层膨胀**：
- 机器人有体积，不能紧贴障碍物
- 多层膨胀提供不同级别的安全距离
- 路径规划时可以选择合适的膨胀层

---

#### 3. `odom_map_tf` - TF变换节点

**文件**：`nav_slam/odom_map_tf.py`

**功能**：
- 动态发布`map → odom`的TF变换
- 将里程计坐标系与地图坐标系关联

**订阅**：
- `/odom` - 机器人里程计数据

**发布**：
- TF变换：`map → odom`

**工作原理**：
```python
1. 接收/odom数据（机器人当前位置）
2. 将odom的位置和姿态作为map→odom的变换
3. 发布TF变换
```

**为什么需要**：
- 路径规划使用`map`坐标系
- 机器人控制使用`odom`坐标系
- 需要TF变换来连接两个坐标系

---

#### 4. `astar` - A*路径规划节点

**文件**：`nav_slam/astar.py`

**功能**：
- 使用A*算法在2D地图上规划从起点到终点的路径
- 对路径进行平滑处理（B样条插值）

**订阅**：
- `/combined_grid` - 2D占用栅格地图
- `/odom` - 机器人当前位置（起点）
- `/goal_pose` - 目标位置（终点）

**发布**：
- `/path` - 原始A*路径
- `/path2` - 平滑后的路径（用于跟踪）

**工作原理**：

**步骤1：接收目标点**
```python
goal_callback() → 保存目标位置 (x, y)
```

**步骤2：A*路径规划**
```python
1. 将机器人当前位置和目标位置转换为地图坐标（格子索引）
2. 调用A*算法：
   - 起点：机器人当前位置
   - 终点：目标位置
   - 地图：combined_grid（障碍物=100，自由空间=1）
3. A*算法搜索：
   - 使用欧几里得距离作为启发式函数
   - 8方向搜索（上下左右+4个对角线）
   - 避开障碍物（值=100的格子）
4. 返回路径点列表
```

**步骤3：路径平滑**
```python
1. 使用B样条（B-Spline）对路径进行平滑
2. 减少路径的尖锐转折
3. 生成更平滑的路径点
```

**A*算法核心**：
```python
# 启发式函数（估计到终点的距离）
heuristic(a, b) = sqrt((ax-bx)² + (ay-by)²)

# 代价函数
cost = 已走距离 + 启发式距离

# 搜索过程
1. 从起点开始
2. 探索相邻8个方向
3. 选择代价最小的格子
4. 重复直到到达终点
5. 回溯构建完整路径
```

**为什么需要路径平滑**：
- A*算法生成的路径可能有尖锐转折
- 机器人无法瞬间改变方向
- 平滑后的路径更适合机器人跟踪

---

#### 5. `start_nav` - 纯追踪路径跟踪节点

**文件**：`nav_slam/start_nav.py`

**功能**：
- 使用纯追踪（Pure Pursuit）算法控制机器人跟随路径
- 发布速度控制指令（`/cmd_vel`）

**订阅**：
- `/path` - A*规划的路径
- `/odom` - 机器人当前位置和姿态

**发布**：
- `/cmd_vel` - 速度控制指令（线速度 + 角速度）

**工作原理**：

**步骤1：接收路径**
```python
path_callback() → 保存路径点列表
→ 对路径进行插值（增加路径点密度）
```

**步骤2：纯追踪控制**
```python
1. 找到离机器人最近的路径点
2. 在前方寻找"前瞻点"（lookahead point）：
   - 距离机器人 >= 前瞻距离（0.5m）
3. 计算转向角：
   steering_angle = atan2(目标点y - 机器人y, 目标点x - 机器人x) - 机器人朝向
4. 根据转向角调整速度：
   - 转向角大 → 速度慢
   - 转向角小 → 速度快
5. 发布控制指令
```

**速度控制策略**：
```python
# 基础速度：0.6 m/s
# 最大速度：1.5 m/s
# 速度公式：
speed = max(0.6, 1.5 - 1.5*sin(0.6*|转向角|))

# 转向角越大，速度越慢
# 转向角接近0，速度接近最大值
```

**停止条件**：
```python
距离终点 < 0.2m → 停止（速度=0，角速度=0）
```

**纯追踪算法原理**：
- **前瞻距离**：机器人总是看向前方一定距离的点
- **几何追踪**：通过几何关系计算转向角
- **优点**：简单、稳定、适合差速机器人

---

## 数据流图

```
┌─────────────┐
│   Gazebo    │
│  仿真环境    │
└──────┬──────┘
       │
       ├─── /points_raw (点云) ───┐
       │                          │
       └─── /odom (里程计) ────────┼──┐
                                  │  │
┌─────────────────────────────────▼──▼──────────────┐
│         points_pub_map 节点                          │
│  将点云从base_link转换到map坐标系                    │
└────────────────────┬───────────────────────────────┘
                     │
                     └─── /mapokk (转换后的点云)
                          │
                          ▼
┌─────────────────────────────────────────────────────┐
│              map_pub 节点                           │
│  将点云转换为2D占用栅格地图                           │
└────────────────────┬────────────────────────────────┘
                     │
                     └─── /combined_grid (2D地图)
                          │
                          ▼
┌─────────────────────────────────────────────────────┐
│              astar 节点                              │
│  A*路径规划 + 路径平滑                                │
└────────────────────┬────────────────────────────────┘
                     │
                     ├─── /path (原始路径)
                     │
                     └─── /path2 (平滑路径)
                          │
                          ▼
┌─────────────────────────────────────────────────────┐
│            start_nav 节点                            │
│  纯追踪路径跟踪控制                                   │
└────────────────────┬────────────────────────────────┘
                     │
                     └─── /cmd_vel (速度指令)
                          │
                          ▼
┌─────────────┐
│   Gazebo    │
│  机器人执行  │
└─────────────┘
```

---

## 运行流程

### 阶段1：启动仿真环境

```bash
ros2 launch gazebo_modele gazebo.launch.py
```

**启动内容**：
1. Gazebo服务器启动，加载3D世界
2. 机器人模型被spawn到世界中
3. 发布基础TF变换（map→odom→base_footprint→base_link）
4. 开始发布传感器数据（点云、里程计）

### 阶段2：启动导航系统

```bash
ros2 launch nav_slam 2dpoints.launch.py
```

**启动内容**：
1. `points_pub_map` - 开始转换点云坐标
2. `map_pub` - 开始构建地图
3. `odom_map_tf` - 开始发布TF变换
4. `astar` - 等待目标点
5. `start_nav` - 等待路径
6. RViz2 - 可视化界面

### 阶段3：建图过程

```
1. 机器人移动（手动或自动）
2. 激光雷达扫描环境 → /points_raw
3. points_pub_map转换坐标 → /mapokk
4. map_pub构建地图 → /combined_grid
5. 地图逐渐完善（障碍物被标记）
```

### 阶段4：路径规划与导航

```
1. 用户在RViz中点击目标点 → /goal_pose
2. astar节点：
   - 接收目标点
   - 获取机器人当前位置（从/odom）
   - 使用A*算法规划路径
   - 平滑路径
   - 发布 /path 和 /path2
3. start_nav节点：
   - 接收路径
   - 使用纯追踪算法计算控制指令
   - 发布 /cmd_vel
4. Gazebo执行控制指令：
   - 机器人开始移动
   - 跟随路径到达目标
```

---

## 关键算法

### 1. A*路径规划算法

**核心思想**：
- 结合了Dijkstra算法（保证最优）和贪心算法（快速）
- 使用启发式函数估计到终点的距离

**算法步骤**：
```
1. 初始化：
   - 开放列表（待探索）：[起点]
   - 关闭列表（已探索）：[]
   - 代价字典：{起点: 0}

2. 循环直到找到终点或开放列表为空：
   a. 从开放列表选择代价最小的点
   b. 将该点移到关闭列表
   c. 如果是终点 → 回溯路径，结束
   d. 否则，探索该点的8个邻居：
      - 如果是障碍物 → 跳过
      - 如果已在关闭列表 → 跳过
      - 计算新代价 = 当前代价 + 移动代价 + 启发式距离
      - 如果新代价更小 → 更新代价，加入开放列表

3. 回溯路径：
   - 从终点开始，沿着"父节点"回溯到起点
   - 反转路径，得到从起点到终点的路径
```

**代码中的实现**：
```python
# 启发式函数
def heuristic(a, b):
    return math.sqrt((a[0] - b[0])**2 + (a[1] - b[1])**2)

# 代价计算
new_cost = cost_so_far[current] + grid[neighbor]
priority = new_cost + heuristic(goal, neighbor)
```

---

### 2. 纯追踪（Pure Pursuit）算法

**核心思想**：
- 机器人总是看向前方一定距离的点（前瞻点）
- 通过几何关系计算转向角

**算法步骤**：
```
1. 找到离机器人最近的路径点
2. 在前方寻找前瞻点：
   - 距离 >= 前瞻距离（lookahead_distance = 0.5m）
3. 计算目标角度：
   target_angle = atan2(目标点y - 机器人y, 目标点x - 机器人x)
4. 计算转向角：
   steering_angle = target_angle - 机器人当前朝向
5. 根据转向角调整速度：
   - 转向角大 → 速度慢
   - 转向角小 → 速度快
```

**几何关系**：
```
       前瞻点
         ●
        /|
       / |
      /  | 前瞻距离
     /   |
    /    |
机器人●──┘
    ↑
  当前朝向
```

**代码中的实现**：
```python
# 找到前瞻点
for i in range(len(path_points)):
    lookahead_point_idx = (closest_point_idx + i) % len(path_points)
    target_point = path_points[lookahead_point_idx]
    distance_to_target = math.sqrt(dx**2 + dy**2)
    if distance_to_target >= self.lookahead_distance:
        break

# 计算转向角
target_angle = math.atan2(dy, dx)
steering_angle = target_angle - vehicle_pose[2]
```

---

### 3. 坐标变换

**为什么需要坐标变换**：
- 激光雷达数据是相对于机器人的（base_link坐标系）
- 建图需要统一坐标系（map坐标系）
- 需要知道机器人在map中的位置和姿态

**变换过程**：
```
1. 获取机器人位姿（从/odom）：
   - 位置：(x, y, z)
   - 姿态：四元数 (qx, qy, qz, qw)

2. 四元数转旋转矩阵：
   - 将四元数转换为3×3旋转矩阵
   - 构建4×4齐次变换矩阵

3. 点云坐标变换：
   - 将点云坐标转换为齐次坐标 [x, y, z, 1]
   - 乘以变换矩阵
   - 提取变换后的坐标
```

**代码中的实现**：
```python
# 四元数转旋转矩阵
qx, qy, qz, qw = quaternion
m00 = 1 - 2*(qy*qy + qz*qz)
m01 = 2*(qx*qy - qw*qz)
# ... 其他矩阵元素

# 构建变换矩阵
rotation_matrix = np.array([
    [m00, m01, m02, translation[0]],
    [m10, m11, m12, translation[1]],
    [m20, m21, m22, translation[2]],
    [0, 0, 0, 1]
])

# 应用变换
transformed_points = (rotation_matrix @ points_homogeneous.T).T[:, :3]
```

---

## 总结

### 系统工作流程

1. **仿真启动** → Gazebo提供传感器数据
2. **坐标变换** → 点云转换到地图坐标系
3. **地图构建** → 点云转换为2D占用栅格地图
4. **路径规划** → A*算法规划路径并平滑
5. **路径跟踪** → 纯追踪算法控制机器人
6. **闭环反馈** → 机器人移动，更新位置，重新规划

### 关键概念

- **TF变换**：连接不同坐标系
- **占用栅格地图**：2D网格表示环境
- **A*算法**：最优路径搜索
- **纯追踪**：路径跟踪控制
- **ROS2话题**：节点间通信

### 学习建议

1. **先理解ROS2基础**：节点、话题、服务、TF
2. **理解坐标系**：map、odom、base_link的关系
3. **理解算法**：A*和纯追踪的原理
4. **调试方法**：使用`ros2 topic echo`查看数据
5. **可视化**：使用RViz2查看地图、路径、TF树

---

**祝你学习顺利！** 🚀

